# Legacy API Version

This folder preserves the original implementation, which calls the OpenAI API directly and requires `OPENAI_API_KEY` or an API key entered in the local interface.

Use the sibling `codex-cli` version if you have ChatGPT Codex access and do not want usage-based API key setup.

To launch this archived version, double-click `start.cmd` and follow the existing interface.
